package main

import (
	"testing"
)

func TestFileServer(t *testing.T) {
	// fmt.Println("_______")
	// files := http.FileServer(http.Dir("/public"))
	// fmt.Println(files)
	// http.Handle("/", files)
	// http.ListenAndServe(":8080", nil)
	// fmt.Println("_______")
}
